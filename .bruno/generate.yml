info:
  name: generate
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/generate"
  body:
    type: json
    data: |-
      {
        "model": "kokoro",
        "input": "Hello, world!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
