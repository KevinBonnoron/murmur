info:
  name: getAll
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/models/:name/voices"
  params:
    - name: name
      value: model
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
