info:
  name: delete
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/api/models/:name"
  params:
    - name: name
      value: model
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
