info:
  name: getOne
  type: http
  seq: 3

http:
  method: GET
  url: "{{base_url}}/api/models/:name"
  params:
    - name: name
      value: model
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
